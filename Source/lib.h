/**
 * @file lib.h
 * @brief Заголовочный файл библиотеки версионирования
 */

#pragma once

/**
 * @brief Получить версию патча проекта
 * 
 * Функция возвращает номер патча (PATCH) версии проекта,
 * который устанавливается при сборке через CMake.
 * 
 * @return Номер патча версии проекта
 */
int version();

